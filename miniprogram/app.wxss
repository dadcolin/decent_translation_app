/**
 * Global Styles for Translation Mini Program
 * 
 * Layout Optimizations Applied:
 * - Apple-style light gray background (#F2F2F7)
 * - Consistent typography using system fonts
 * - Minimal, clean design aesthetic
 */

page {
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', 'Arial', sans-serif;
  color: #1a1a1a;
  background: #F2F2F7;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* Common button reset */
button {
  margin: 0;
  padding: 0;
  background: transparent;
  border: none;
  outline: none;
}

button::after {
  border: none;
}

/* Common input reset */
input, textarea {
  margin: 0;
  padding: 0;
  background: transparent;
  border: none;
}

/* Hide scrollbar but keep scrolling */
::-webkit-scrollbar {
  display: none;
}
